; Unreal-Game-Template — Editor-only configuration.
; See Config/DefaultEngine.ini's header note on hand-authored ini caveats.

[/Script/UnrealEd.EditorPerProjectUserSettings]
; Keep source control prompts out of the way for a fresh template clone —
; remove/flip once the project has real Perforce/Git-in-editor workflows set up.
bSourceControlCompatible=True

[/Script/UnrealEd.ProjectPackagingSettings]
; Sane packaging defaults for the template; revisit per-platform before
; first real Shipping build.
BuildConfiguration=PPBC_Development
UsePakFile=True
