# Infra/cloud/aws/cfn-template.yaml
# CloudFormation template for a serverless infrastructure stack.

AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Serverless Backend stack for the Unreal Game Template.

Resources:
  # DynamoDB Database Table
  Unreal_game_templateTable:
    Type: AWS::Serverless::SimpleTable
    Properties:
      PrimaryKey:
        Name: id
        Type: String
      ProvisionedThroughput:
        ReadCapacityUnits: 5
        WriteCapacityUnits: 5

  # Lambda Function
  Unreal_game_templateFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: handler.lambda_handler
      Runtime: python3.11
      CodeUri: ./src
      Environment:
        Variables:
          TABLE_NAME: !Ref Unreal_game_templateTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref Unreal_game_templateTable
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /status
            Method: get
