# Roadmap: Architecture & Tooling

## 1. Asset Pipeline

Track the plan for content creation/import pipeline (art, audio, level design) as the project scales past placeholder geometry.

## 2. Persistence

Save/load system design — what state needs to persist, where (local disk vs. cloud save), and how it's versioned across game updates.

## 3. Progression / Meta Systems

Placeholder for any cross-session progression (unlocks, currency, profile stats) — scope this once the core loop (Phase 1) is validated, not before.

## 4. CPU and Memory Hardening

Track profiling passes and their findings here as the actor count/scene complexity grows past prototype scale.

## 5. Scope-Creep Monitoring

[Research] An LLM-based process comparing commit history against the design doc/roadmap to flag scope creep early — speculative, revisit once there's enough commit history to make it useful.
